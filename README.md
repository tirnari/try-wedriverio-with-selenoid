# try-wedriverio-with-selenoid

##### Selenoid  — selenium hub successor running browsers within containers.

#### + browser language settings

### How to run tests:

- `yarn`
- `./ci/ci.sh`
- open selenoid-ui `http://localhost:8080/`